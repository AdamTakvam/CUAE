This sample illustrates creation of Certificate Requests 
(TElCertificateRequest), also called Certificate Signing Requests.

Bu default the sample saves the private key in PEM (text) format, which should
be then read using TElX509Certificate.LoadKeyFromPEM() method.